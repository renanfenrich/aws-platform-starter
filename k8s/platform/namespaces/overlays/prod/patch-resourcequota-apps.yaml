apiVersion: v1
kind: ResourceQuota
metadata:
  name: apps-baseline
  namespace: apps
spec:
  hard:
    requests.cpu: "8"
    requests.memory: "16Gi"
    limits.cpu: "16"
    limits.memory: "32Gi"
    pods: "120"
    services: "30"
    configmaps: "200"
    secrets: "200"
